package waclay-wasi:examples@0.1.0;

interface funcs {
    get-env-var-all: func() -> list<tuple<string, string>>;
    get-env-var: func(name: string) -> option<string>;
    get-program-name: func() -> string;
    get-cli-args: func() -> list<string>;
}

world env {
    export funcs;
}
